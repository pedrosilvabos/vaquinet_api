# General oPastor application client (anon/public key as appropriate).
SUPABASE_OPASTOR_URL=
SUPABASE_OPASTOR_KEY=

# Server-only. Required for collar registry reads/writes; never expose to a Base,
# browser, mobile app, or committed .env file.
SUPABASE_OPASTOR_SERVICE_ROLE_KEY=
